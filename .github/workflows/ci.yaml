name: CI

on:
  push:
    branches:
      - main
      - feature/project-setup

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "GitHub Actions is working!"